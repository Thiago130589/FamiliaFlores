// Inicialização do Firestore
// Adicione este arquivo ao seu HTML após o firebase-init.js

// Importa o Firestore SDK (via CDN, adicione a tag <script> no HTML)
// firebase.firestore() já estará disponível
