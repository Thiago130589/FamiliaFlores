// Este arquivo script.js contém funções JavaScript auxiliares reutilizáveis.

// A inicialização do Firebase e a criação da instância do Firestore (db)
// devem estar no firebase-init.js para evitar conflitos.

// Removido o código do Firebase SDK v9 para evitar conflito com o SDK v8 do index.html.

// Você pode adicionar funções auxiliares globais aqui, como:
/*
function formatarMoeda(valor) {
    return 'R$ ' + (valor || 0).toFixed(2).replace('.', ',');
}
*/